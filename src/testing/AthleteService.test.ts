import { describe, it } from "vitest"

describe("AthleteService", () => {
  it.todo("creates an athlete when the name is unique")
  it.todo("fails with DuplicateAthleteName when the trimmed name already exists")
})

