import { describe, it } from "vitest"

describe("CheckInService", () => {
  it.todo("records a check-in when the athlete exists and is below the limit")
  it.todo("fails with AthleteNotFound when the athlete does not exist")
  it.todo("fails with AthleteCheckInLimitReached when there are already 20 check-ins")
  it.todo("lists check-ins for an existing athlete")
})
